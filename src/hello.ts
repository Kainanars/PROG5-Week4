// Declaração de variável com tipo explícito string
const mensagem: string = 'Hello World!';

// Função que recebe dois números e retorna sua soma
function soma(a: number, b: number): number {
  return a + b;
}

console.log(mensagem);
console.log(`A soma de 3 e 5 é: ${soma(3, 5)}`);
